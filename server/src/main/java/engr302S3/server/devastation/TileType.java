package engr302S3.server.devastation;

/**
 * Enum representing the possible types of content that a tile can hold.
 */
public enum TileType {
  EMPTY,     // No content on the tile
  STATION,   // Tile contains a Station
  PLAYER,    // Tile contains a Player
  TICKET     // Tile contains a Ticket
}
